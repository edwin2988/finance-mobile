# 🚀 陈大哥理财账本 - 一键部署指南

## 当前状态
- ✅ 代码已提交到本地 Git
- ✅ GitHub Actions 配置已就绪
- ✅ buildozer.spec 配置已优化
- ⏳ 需要推送到 GitHub 触发自动构建

## 📋 快速部署步骤

### 方法 1：使用 GitHub 网页版（最简单！）

1. **打开 GitHub 创建仓库**
   - 访问：https://github.com/new
   - 仓库名：`finance-mobile`
   - 可见性：Public（公开）
   - 点击 "Create repository"

2. **获取推送命令**
   - 创建后，GitHub 会显示推送命令
   - 复制 "push an existing repository" 部分的命令

3. **生成 GitHub Token**
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选权限：`repo` (全部勾选)
   - 生成后保存 token

4. **推送代码**
   ```bash
   cd /home/openclaw/.openclaw/workspace/理财记账手机版
   git remote set-url origin https://<你的token>@github.com/edwin2988/finance-mobile.git
   git push -u origin main
   ```

5. **等待自动构建**
   - 打开仓库的 Actions 标签
   - 等待构建完成（约 10-20 分钟）
   - 下载 APK

### 方法 2：使用 GitHub CLI（推荐）

```bash
# 安装 GitHub CLI
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
sudo apt update
sudo apt install gh

# 登录 GitHub
gh auth login

# 推送代码
cd /home/openclaw/.openclaw/workspace/理财记账手机版
git push -u origin main
```

## 📱 构建完成后

1. 打开 GitHub 仓库的 **Actions** 标签
2. 点击最新的构建记录
3. 在页面底部找到 **Artifacts**
4. 点击 **finance-app** 下载
5. 解压 ZIP 文件得到 APK
6. 发送到手机安装

## 🎯 当前项目文件

```
理财记账手机版/
├── main_kivy.py          # 主程序（Kivy 版本）
├── main_flet.py          # Flet 版本
├── main.py               # 原始版本
├── main_streamlit.py     # Streamlit 版本
├── buildozer.spec        # APK 打包配置
├── requirements.txt      # Python 依赖
├── data/
│   └── finance_data_v5.json  # 示例数据
├── .github/workflows/
│   └── build.yml         # GitHub Actions 配置
└── README.md             # 项目说明
```

## 📞 需要帮助？

如果遇到问题，请告诉我：
- GitHub 账号：edwin2988
- 仓库名：finance-mobile
- 需要推送的分支：main

**最后更新：** 2026-03-30
